# 2026-03-13

## Short-Term Decisions
- Established repo governance files: `AGENTS.md`, `TODO.md`, and date-based note format.
- Defined boundary model: OpenClaw interacts with `oc-companion` over Unix domain sockets.
- Confirmed event callback path: `oc-companion` invokes OpenClaw webhooks for notifications.
- Added startup context rule: agents read the two most recent note files before beginning work.
- Chose Go as the primary implementation language.
- Added initial Go service skeleton with signal-aware startup/shutdown path.
- Added environment-driven config loading and structured logging bootstrap.
- Added Unix domain socket listener bootstrap and stale socket handling.
- Validated scaffold with `gofmt` and `go build` (using workspace-local Go caches in this environment).

## Direction Snapshot
- Build `oc-companion` as a separate process and Linux user.
- Keep secrets and provider credentials isolated in `oc-companion`.
- Expose only minimal tool interfaces needed by OpenClaw.

## Initial Scope Captured
- Gmail new-message notification via GCP Pub/Sub.
- Gmail message retrieval tool for OpenClaw.
- Google Calendar events retrieval tool for OpenClaw.

## Notes
- Use this file pattern for ongoing short-term project notes: `./.notes/YYYY-MM-DD.md`.
- Current `TODO.md` in-progress item moved to socket API contract definition.
