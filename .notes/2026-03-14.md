# 2026-03-14

## Short-Term Decisions
- Coordinated work using git workspaces under `./workspaces/` to separate protocol and tool-method tracks.
- Chose a minimal socket interaction model for clients:
  1) connect to Unix socket,
  2) call `system.discover`,
  3) invoke tool methods with discovered metadata,
  4) reuse the same connection for additional requests.
- Added `system.discover` as a discovery endpoint for method and usage metadata.
- Extended discovery metadata with asynchronous event contracts for webhook-delivered notifications (`gmail.new_message`).
- Added initial tool method contracts (`gmail.getMessage`, `calendar.listEvents`) with placeholder integrations.
- Updated protocol request ID handling to support string or numeric IDs transparently.

## Implementation Snapshot
- Added socket protocol docs at `docs/socket-protocol.md`.
- Added API registry/dispatch layer and per-connection JSON stream server.
- Added method validation and normalized protocol error responses.
- Added unit tests for registry dispatch and tool-method validation.

## Coordination Notes
- Performed coordinator review after first-pass sub-agent delivery.
- Assigned and integrated revision pass focused on protocol ergonomics and discovery metadata quality.
